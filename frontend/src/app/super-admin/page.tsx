"use client";
import {useEffect,useState} from "react";import{useRouter}from"next/navigation";import api from"@/lib/api";import Sidebar from"@/components/Sidebar";import{getSession}from"@/lib/auth";
export default function SuperAdminDashboard(){const router=useRouter();const[s,setS]=useState<any[]>([]);const[l,setL]=useState(true);useEffect(()=>{getSession().then(x=>{if(x.role!=="super_admin"){router.push("/login");return}load()}).catch(()=>router.push("/login"))},[router]);const load=async()=>{try{const r=await api.get("/api/super-admin/societies");setS(r.data||[])}finally{setL(false)}};if(l)return <div className="flex h-screen items-center justify-center text-gray-500">Loading Super Admin...</div>;return <div className="flex h-screen overflow-hidden bg-[#0a0e17]"><Sidebar role="super_admin"/><main className="flex-1 overflow-y-auto p-6 pt-20"><h1 className="text-2xl font-bold text-white mb-6">All Societies</h1>{s.map(x=><section key={x.id} className="bg-gray-900/80 border border-gray-800 rounded-xl p-6 mb-6"><div className="flex justify-between"><div><h2 className="text-lg font-bold text-white">{x.name}</h2><p className="text-xs text-gray-500">{x.location}</p></div><span className={x.pi_online?"text-emerald-400":"text-red-400"}>{x.pi_online?"ONLINE":"OFFLINE"}</span></div>{(x.devices||[]).map((d:any)=><div key={d.id} className="mt-5 border-t border-gray-800 pt-4"><div className="text-sm font-semibold text-white">{d.name}</div><div className="text-[10px] text-gray-500 font-mono">{d.id}</div><div className="grid grid-cols-1 md:grid-cols-4 gap-3 mt-3">{["A","B","C","D"].map(k=>{const c=d.slots?.[k];if(!c)return null;return <div key={k} className="border border-gray-800 rounded p-3 text-xs"><b className="text-white">{c.display_name||`Slot ${k}`}</b><div className="text-gray-400 mt-2">Target {c.target_days}d · Used {c.used_days}d</div><div className="text-gray-400">Physical {c.physical_toggle}</div></div>})}</div></div>)}</section>)}</main></div>}
